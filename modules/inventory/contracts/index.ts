export { ProductSchema, MovementSchema } from "@suite/contracts";
export type { Product } from "@suite/contracts";
export const inventoryManifest = {
  id: "inventory",
  dependencies: [],
  version: 1,
} as const;
