# SDK-04 acceptance map

Audit of the approved typed authoring, generated documentation/UI and composition requirements, 17 September 2026. SDK-04 was locally verified within the scope below and is now reopened on 18 September for SDK-04-FMT. Evidence here does not establish whole-platform parity or final UI approval.

| Requirement                                                  | Current evidence                                                                                                                                                                                                                                                                                                         | Remaining acceptance                                                                                                                                                                                           |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Declare resource schemas once and infer clients/forms/tables | `createModuleClient`, public `ResourceClient`, `SchemaDraft`, `TypedSchemaForm`, `TypedResourceTable`; [form acceptance](verification/schema-forms/README.md) and [resource lists](verification/resource-lists/README.md). Current strict compilation and full unit suite exercise negative inferred contracts.          | [Public client results](verification/resource-response/README.md) and [generated host/cache/journal responses](verification/generated-response/README.md) pass validation acceptance.                          |
| Infer operation inputs, outputs and declared errors          | `createModuleClient.call/attempt`, `OperationContext`, `defineModuleServer`; [service contract checks](../tests/service-contracts.test.ts) and current `tests/module-services.test.ts`/`tests/module-queries.test.ts`.                                                                                                   | Current full-suite acceptance preserves inferred operation errors and rejects malformed resource adapter results. Broader durable custom-operation recovery remains OFF-03.                                    |
| Infer configuration, event payloads and public services      | Schema-derived `Configuration`, `ModuleEvents`, `ModuleServices` and scoped context validation; current `tests/module-services.test.ts`, `tests/service-contracts.test.ts` and generated-documentation compilation.                                                                                                      | Wider host capability grants remain SDK-05; this evidence does not authorize undeclared cross-module or desktop access.                                                                                        |
| Generate documentation from the actual contracts             | [Schema-derived references](verification/module-documentation/README.md); CLI/build parity, stale-reference detection, exact transported schemas and compiled generated examples in current `tests/module-documentation.test.ts`.                                                                                        | [Resource response semantics](module-resource-responses.md) now document validation, capability revision and uncertainty. [Generated host integration](verification/generated-response/README.md) is verified. |
| Generate validated forms and compose custom views            | [Structured forms](verification/schema-forms/README.md), [tuple/map editors](verification/map-tuple/README.md), [reference fields](verification/reference-fields/README.md), [reference clients](verification/reference-client/README.md). Dedicated editors and explicit validated JSON cover documented schema shapes. | Complete UI-kit component inventory and broad accessibility journeys remain UI-01/UI-02, not implicitly accepted by these scoped form tests.                                                                   |
| Generate filtering, sorting and stable pagination            | [Ranges](verification/resource-ranges/README.md), [sorting](verification/resource-sort/README.md), [resource queries](verification/resource-query/README.md) and [read-only query composition](verification/query-context/README.md). Public read-only server queries now share the complete inferred list contract.     | Public clients and generated live/cache/journal paths validate responses. Large-registry performance remains OPS-07.                                                                                           |
| Resolve scoped resource references and structured values     | [Write integrity](verification/reference-integrity/README.md), [local migration references](verification/local-migration-references/README.md), [structured tables](verification/table-labels/README.md).                                                                                                                | Cross-module standalone grants remain SDK-05. Broader import/recovery requirements retain their OFF/ID rows.                                                                                                   |
| Install independent typed views without central host edits   | [Signed executable modules](verification/executable-modules/README.md), [host UI contracts](verification/host-ui/README.md), [read-only query composition](verification/query-context/README.md). Current independent CLI publication and headless/native execution pass.                                                | Complete fifth-module lifecycle, hosted signing and production-platform acceptance remain EXT/OPS gates.                                                                                                       |

## Final gate: generated host responses and journal acknowledgment

[Generated response acceptance](verification/generated-response/README.md) closes the separate direct-transport paths in `packages/shell/src/module-view.tsx` and `syncModuleStorage`. Live/cached results are validated; queues retain each original signed module/resource/version contract rather than using the currently displayed release. Malformed responses remain pending, preserve keys, block dependent work and allow unrelated work to continue.

The final audit checked the public resource/query client and response schemas, generated schema editors/tables/reference controls, schema-derived documentation generation, inferred configuration/event/error/service contracts and independent signed custom-view composition. Current strict compilation includes negative contract assertions. The full 235-test unit/PostgreSQL run, 20 headless browser journeys and four hidden native journeys preserve those scoped paths and verify the new integration.

SDK-04 is locally verified. This does not close SDK-05 host/standalone capability grants, OFF-03 arbitrary durable-operation recovery, UI-01/02/04 full kit and accessibility acceptance, EXT fifth-module lifecycle, OPS performance/signing/production-platform gates, or the separately queued final UI refinement goal. No broader requirement was deferred or marked complete by this scoped acceptance.

## Next primary SDK work: SDK-05

Audit the public view/worker contexts and privileged host methods, then implement typed manifest-declared capability requests and module-scoped grants with current account/workspace checks. Include desktop/LAN interfaces and cross-module standalone access. Verify denied, undeclared, revoked and stale-context calls at the host boundary; existing React/UI compatibility revisions are not authority grants.

## Reopened standard-format validation, 18 September 2026

The administrator-review integration exposed that portable `assertSchema(Type.String({ format: "uuid" }), validUuid)` rejects a valid value with `Unknown format`. Direct checks also reproduce the same error for valid email, URI and date values; log `/tmp/gabs-sdk-format-reproduction.log`. Generated editors already recognize these formats, so the current authoring and validation behavior is inconsistent. An explicit-pattern correction fixes organization policy IDs, not the general SDK contract.

**SDK-04-FMT remains ready, unimplemented and required.** Define a documented supported-format contract shared by portable runtime validation, authoritative requests, local workers, simulator and generated forms. Prove valid and invalid standard-format values across those environments, preserve the declared schema in generated references/packages, and reject unsupported formats with actionable authoring diagnostics rather than silently ignoring them. Preserve strict schema and mutation/receipt behavior. Existing acceptance above remains valid only for its tested scope; SDK-04 is not complete until this gate passes.
