# Corporate device lease client store

18 September 2026. SDK-05 remains active. This verifies the reusable client guard and browser persistence adapter. At this milestone product views still required online device authorization. Subsequent [browser integration](../browser-capability-leases/README.md) now verifies cached-view exports; Electron offline authority remains unfinished.

## Implemented boundary

- [Client lease store](../../../packages/client/src/identity/capability-leases.ts) binds grants to account, workspace, exact signed module contract and capability. It accepts authority only from a host-owned trusted transport callback and verifies signatures before retention and each use. Stored grants alone cannot recover a clock fault.
- [Browser adapter](../../../packages/client/src/adapters/browser.ts) uses IndexedDB under the existing account/workspace namespace. An account Web Lock serializes lease checks and workspace/account purges across cooperating tabs. A check already verifying a signature cannot restore a lease after another tab finishes purging it. The Electron renderer cache allowlist has not been widened to accept corporate authority records.
- Connected policy observations discard obsolete grants, preserve the highest known revision and reject delayed renewal responses. A denied policy revision cannot be re-enabled by an older or equal observation. Explicit invalidation removes retained grants and invalidates pending checks.
- Prepared actions bind to a particular lease identifier and recheck live host access, scope, policy, signature, expiry and device opt-in. Hosts must call the returned guard immediately before effects and after asynchronous dialogs. Replacing a lease does not silently reauthorize an already prepared effect.
- Persisted time high-water marks and monotonic elapsed time prevent known clock rollback from extending a grant. A one-second wall-clock adjustment tolerance avoids incidental subsecond drift; effective time never moves backwards during offline checks. Detected clock faults remain blocked until newly verified connected renewal. Clock or expiry failures learned during a slow storage write are persisted before rejection. This does not claim security against a compromised operating system or rewritten host storage.
- The authenticated key endpoint now supplies its independently configured normalized issuer alongside its public key/fingerprint. The client must obtain that authority through the trusted API transport, never derive it from a module-supplied token. API schemas were regenerated.

## Executed acceptance

| Check                                                               | Result and scope                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused SDK/client/server/workspace-policy regression               | 28 tests across eight files passed on a fresh migrated/seeded PostgreSQL database. Includes 11 client guard tests for restart, isolation, tampering, policy races, key rotation, profile closure, storage failure, lease expiry and clock rollback. Log: `/tmp/gabs-client-leases-acceptance-final.log`.                                                                                                                          |
| Actual IndexedDB and Web Locks in headless Chromium                 | Page termination/restart, cross-tab revocation, reauthorization and account/workspace purge isolation passed. A held real signature verification proved that a concurrent purge waits for the account lock and leaves no restored grant. The test bundles the actual adapter into an isolated browser harness and supplies test-signed grants. It does not claim authenticated application acquisition or offline device effects. |
| Existing online custom-module export regression                     | Real download, live permission denial, stale-view rejection and foreign/undeclared API rejection passed. Both browser tests completed in one run. Log: `/tmp/gabs-client-leases-browser.log`. Historical screenshot artifacts were restored after the regression.                                                                                                                                                                 |
| Strict types, environment graphs, dependency/copy checks and builds | Passed; all four production bundles built. Log: `/tmp/gabs-client-leases-build.log`.                                                                                                                                                                                                                                                                                                                                              |
| API generation                                                      | Generated OpenAPI and inferred client schema with the issuer field. Log: `/tmp/gabs-client-leases-api.log`.                                                                                                                                                                                                                                                                                                                       |

Each completed isolated runner removed its own database. No Electron process or foreground browser was launched for this milestone. There are no visual changes requiring new product screenshots. Earlier counts in the server-authority record are historical, not additive.

## Next acceptance

Wire actual online key acquisition/renewal and connected policy delivery into the browser host. Do not fall back to cached grants after known server denials. Observing a rotated trusted key must invalidate earlier authority even if subsequent issuance fails. Check current profile/view state after acquisition and asynchronous writes. Prefetch opted-in capabilities from installed signed contracts without needing synthetic operation input.

Then implement main-process-owned encrypted authority storage and independently verified native offline effects. Verify expiry/revocation after file dialogs, profile restart/lock, current accepted release, disabled offline access and cached custom views in real browser and hidden Electron journeys. Do not trust renderer-provided roles, keys, leases or offline flags at the privileged native boundary. Keep LAN/business commitments outside these leases. Full SDK-05, full parity and final UI refinement remain open.

The later [browser integration milestone](../browser-capability-leases/README.md) completes the browser acquisition/policy/effect work above and adds immediate key-rotation invalidation plus durable-write denial races. Its scoped acceptance supersedes this record for current browser behavior. Issuer-wide trust propagation across cached workspaces/accounts, followed by native offline authority, remains required.
